<div id="auth">
    <h1>Sign In</h1>
    <div id="formAndQr">
        <div class="row">
            <h4 class="col-sm-12">with Username/Email</h4>
            <form id="signInForm" method="post" onsubmit="App.run();return false;">
                <div class="form-group row">
                    <div class="col-sm-12">
                        <input type="email" class="form-control" name="email" id="email">
                        <input type="hidden" class="form-control" name="redirectUrl" id="redirectUrl" value="{{url}}">
                        <button type="button" class="btn btn-primary col-sm-12" id="signInBtn" onclick="App.run();return false;">Sign in</button>
                    </div>
                </div>

            </form>
        </div>
        <h4>or use BiomioID</h4>
        <div id="qrcode">
        </div>
    </div>
    <div>
        <div id="timer"></div>
        <div class="message__holder">
            <div id="message"></div>
        </div>
        <div class="footer">
            <img class="footer__logo" src="footer-logo.png" alt="biomio logo" title="" />
            <p class="footer__text">Multi-Factor, Password-Optional Authentication Anywhere, Everywhere</p>
        </div>
    </div>

</div>

<script src="/socket.io/socket.io.js"></script>
<script src="/js/auth.js"></script>
<script src="/js/jquery.qrcode.js"></script>
<script src="/js/qrcode.js"></script>
<script>
    $(function () {
        App.init({
            flow: 'token',
            idHolder: $('#email'),
            messageHolder: $('#message'),
            timerHolder:  $('#timer'),
            redirectUrl: $('#redirectUrl').val(),
            qrHolder: $('#formAndQr'),
            sessionID: '{{sessionID}}'
        });
    });
    $('.message__holder, #timer').hide();
    $('#qrcode').qrcode({
        text: '{{qrUrl}}',
        width: 150,
        height: 150
    });

</script>